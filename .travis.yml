sudo: required
language: node_js

services:
  - "docker"

stages:
  - test
  - push

jobs:
  include:
    - stage: test
      name: "Unit tests"
      install:
        - npm install
      script:
        - npm test

    - stage: push
      name: "Push to registry"
      if: tag IS present
